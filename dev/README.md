# Dev-only pages

This folder holds local prototype and visual test pages that are not part of the public static-site navigation.

Public page sources now live under `pages/`. Run `npm run pages:build` to
create `_site/`, which preserves flat public routes such as `index.html`,
`forge-quiz.html`, `teacher.html`, and the subject landing pages.
