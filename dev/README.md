# Dev-only pages

This folder holds local prototype and visual test pages that are not part of the public static-site navigation.

Keep public route files such as `index.html`, `forge-quiz.html`, `teacher.html`, and subject landing pages at the repository root unless you also add redirects or update deployment routing.
