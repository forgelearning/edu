# Public runtime files

These files are source assets that must be published at the site root for
browser and PWA compatibility. `npm run pages:build` copies them into `_site/`
without changing their public URLs.
